import createService from "../utils/apiService";

let entregasService = createService("entregas");

entregasService.create = function () {
    // CONECTARSE CON API EXTERNO -- faas
};

export default entregasService;
